# NewsEngine Docker Compose
# ========================
# 部署 NewsEngine 及其依赖（Neo4j 图数据库）
#
# 启动: docker compose up -d
# 停止: docker compose down
# 日志: docker compose logs -f
#
# 环境变量说明:
#   NEO4J_AUTH=neo4j/<password>  设置 Neo4j 密码
#   数据持久化至 ./data/neo4j/

services:
  neo4j:
    image: neo4j:5-community
    container_name: newsengine-neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"   # Neo4j Browser / HTTP API
      - "7687:7687"   # Bolt 协议
    environment:
      NEO4J_AUTH: neo4j/newsengine2026
      NEO4J_server_memory_heap_max__size: 2g
      NEO4J_server_memory_heap_initial__size: 512m
      NEO4J_server_memory_pagecache_size: 512m
    volumes:
      - ./data/neo4j/data:/data
      - ./data/neo4j/logs:/logs
      - ./data/neo4j/import:/var/lib/neo4j/import
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # TODO: 后续添加 NewsEngine 主服务
  # app:
  #   build: .
  #   container_name: newsengine-app
  #   restart: unless-stopped
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     neo4j:
  #       condition: service_healthy
  #   environment:
  #     NEO4J_URI: bolt://neo4j:7687
  #     NEO4J_USER: neo4j
  #     NEO4J_PASSWORD: newsengine2026
  #     LLM_API_KEY: ${LLM_API_KEY:-}
  #     LLM_BASE_URL: ${LLM_BASE_URL:-https://api.deepseek.com}
  #     LLM_MODEL: ${LLM_MODEL:-deepseek-chat}
  #     EMBEDDING_API_KEY: ${EMBEDDING_API_KEY:-}
  #     EMBEDDING_BASE_URL: ${EMBEDDING_BASE_URL:-}
  #     EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-v3}
  #   volumes:
  #     - ./data/newsengine:/app/data
